export const bands={coverageDepth:{poor:.35,watch:.5,healthy:.65,elite:.8},remarketingLoad:{best:10,healthy:20,high:25,severe:35},serviceTouchIndex:{lowFriction:45,healthy:75,heavy:120},benchScore:{intervene:40,developing:60,healthy:80}};export function bandColor(metric:string,value:number){switch(metric){case'coverageDepth':if(value>=bands.coverageDepth.elite)return'text-leader';if(value>=bands.coverageDepth.healthy)return'text-good';if(value>=bands.coverageDepth.watch)return'text-warn';return'text-bad';case'remarketingLoad':if(value<=bands.remarketingLoad.best)return'text-leader';if(value<=bands.remarketingLoad.healthy)return'text-good';if(value<=bands.remarketingLoad.high)return'text-warn';return'text-bad';case'serviceTouchIndex':if(value<=bands.serviceTouchIndex.lowFriction)return'text-leader';if(value<=bands.serviceTouchIndex.healthy)return'text-good';if(value<=bands.serviceTouchIndex.heavy)return'text-warn';return'text-bad';case'benchScore':if(value>=bands.benchScore.healthy)return'text-good';if(value>=bands.benchScore.developing)return'text-warn';return'text-bad';default:return'text-slate-200';}}