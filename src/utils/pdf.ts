import html2canvas from 'html2canvas';import jsPDF from 'jspdf';export async function exportElementToPDF(element:HTMLElement,filename='bundlebench-snapshot.pdf'){const canvas=await html2canvas(element,{scale:2,backgroundColor:'#0b1222'});const imgData=canvas.toDataURL('image/png');const pdf=new jsPDF({orientation:'portrait',unit:'pt',format:'a4'});const pageWidth=pdf.internal.pageSize.getWidth();const pageHeight=pdf.internal.pageSize.getHeight();const imgWidth=pageWidth-64;const imgHeight=canvas.height*(imgWidth/canvas.width);let y=32;if(imgHeight>pageHeight-64){let hLeft=imgHeight;let position=32;while(hLeft>0){pdf.addImage(imgData,'PNG',32,position,imgWidth,imgHeight);hLeft-=pageHeight;if(hLeft>0){pdf.addPage();position=0}}}else{pdf.addImage(imgData,'PNG',32,y,imgWidth,imgHeight)}pdf.save(filename)}