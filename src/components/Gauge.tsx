type Props={value:number;max?:number;label?:string};export default function Gauge({value,max=100,label}:Props){const pct=Math.max(0,Math.min(100,value/max*100));return(<div className='card'>{label&&<div className='mb-2 text-sm text-slate-300'>{label}</div>}<div className='relative h-3 w-full rounded-full bg-white/10'><div className='absolute left-0 top-0 h-3 rounded-full bg-indigo-400' style={{width:`${pct}%`}}/></div><div className='mt-2 text-sm'>{Math.round(value)} / {max}</div></div>)}