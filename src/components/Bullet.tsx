type Props={value:number;target:number;goodIsLow?:boolean;label?:string};export default function Bullet({value,target,goodIsLow=false,label}:Props){const span=Math.max(value,target);const pctVal=value/span*100,pctTar=target/span*100;return(<div className='card'>{label&&<div className='mb-2 text-sm text-slate-300'>{label}</div>}<div className='relative h-3 w-full rounded bg-white/10'><div className='absolute inset-y-0 left-0 bg-indigo-400' style={{width:`${pctVal}%`}}/><div className={`absolute -top-1 h-5 w-0.5 ${goodIsLow?'bg-emerald-400':'bg-amber-300'}`} style={{left:`${pctTar}%`}}/></div><div className='mt-2 text-xs text-slate-400'>Value: {Math.round(value)} • Target: {target} {goodIsLow?'(lower is better)':'(higher is better)'}</div></div>)}