import{bandColor}from'../config/benchmarks';type Props={label:string;value:string;metric?:string;numeric?:number;badge?:string;tooltip?:string};export default function Kpi({label,value,metric,numeric,badge,tooltip}:Props){const cls=numeric!=null&&metric?bandColor(metric,numeric):'text-slate-100';return(<div className='card'><div className='flex items-center justify-between'><div className='text-sm text-slate-300'>{label}</div>{badge&&<span className='badge border-white/10'>{badge}</span>}</div><div className={`kpi ${cls}`}>{value}</div>{tooltip&&<div className='mt-1 text-xs text-slate-400'>{tooltip}</div>}</div>)}